# Ruby on Rails Simple Login

### Install Bundles

    $ bundle install

### Run Migrations:

    $ bin/rails db:migrate RAILS_ENV=developement

### Run the server:

    $ bin/rails server

### Go to locahost:3000.